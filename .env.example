# OpenAI-compatible provider for the last pipeline layer.
# Optional: without it the pipeline runs four layers and reports how much
# traffic would have reached the fifth. Keep secrets in .env.local and Vercel
# settings — the key is read on the server only and never reaches the browser.
AI_BASE_URL=
AI_API_KEY=
AI_CHAT_MODEL=
