# Normalize every text file to LF in the repository and in the working tree.
# Without this a checkout on Windows or a copy through a non-POSIX filesystem
# rewrites whole files as CRLF and buries real diffs in noise.
* text=auto eol=lf

*.ico binary
*.png binary
*.jpg binary
*.pdf binary
*.mp4 binary
